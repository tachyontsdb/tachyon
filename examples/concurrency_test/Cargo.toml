[package]
name = "concurrency_test"
version = "0.1.0"
edition = "2021"

[dependencies]
tachyon_core = { path = "../../tachyon_core" }
rustix = { version = "0.38.4", features = ["fs"] }
rand = "0.8"
chrono = "0.4"
